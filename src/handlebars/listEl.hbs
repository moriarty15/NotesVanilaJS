{{#each getStore}}
<li class="notes__item" id={{id}}>

        <p class="notes__item__text">{{text}}</p>

    <div class="notes__item__btn">
        <button class="btn delete">delete</button>
        <button class="btn change">change</button>
    </div>
</li>
{{/each}}